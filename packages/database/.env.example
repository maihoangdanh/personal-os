# Supabase Postgres — direct connection (dùng cho Prisma Migrate + runtime NestJS)
# Lấy tại: Supabase Dashboard → Connect → URI → Direct connection
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"
